import { DataBodyMeasurements } from './data-body-measurements';

describe('DataBodyMeasurements', () => {
  it('should create an instance', () => {
    expect(new DataBodyMeasurements()).toBeTruthy();
  });
});
